version: "3.9"

services:
  llm-prompt-router:
    build:
      context: ..
      dockerfile: docker/Dockerfile

    container_name: llm_prompt_router

    ports:
      - "8000:8000"

    env_file:
      - ../.env

    volumes:
      - ../logs:/app/logs

    restart: unless-stopped